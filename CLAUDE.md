# gpt-term / Scrollback — 작업 지침

`chatgpt.com` 을 터미널 TUI 로 갈아끼우는 개인용 Chrome MV3 확장이다.
아키텍처·명령어·테스트 목록은 [`README.md`](README.md) 에 있다.

---

## 1. Git — 이 저장소의 규칙

**전역 지침(`~/.claude/CLAUDE.md` §4)의 git 항목을 이 저장소에서는 아래로 대체한다.**

### main 에 직접 올린다

- **브랜치를 따지 않는다.** 작업은 전부 `main` 에서 하고 `main` 에 올린다.
- 전역의 "기본 브랜치에 직접 커밋하지 않는다" 는 여기서는 적용하지 않는다.

### 작업이 끝나면 바로 커밋하고 바로 푸시한다

- **따로 말하지 않아도 커밋·푸시한다.** 매번 물어보지 않는다.
- 전역의 "요청하지 않으면 커밋·푸시하지 않는다" 는 여기서는 적용하지 않는다.
- 한 덩어리가 끝날 때마다 올린다. 여러 작업을 모아 두었다가 한꺼번에 올리지 않는다.

### 그래도 지키는 것

- **커밋은 원자적 단위로 나눈다.** "바로 올린다" 가 "아무렇게나 뭉쳐 올린다" 는 뜻은 아니다.
- **올리기 전에 `tools/test.sh` 가 통과해야 한다.** 깨진 것을 올리지 않는다.
- 커밋 메시지는 한국어로, **무엇을 왜 고쳤는지** 를 적는다. 실측값이 있으면 같이 남긴다.
- 되돌릴 수 없는 다른 작업(강제 푸시, 히스토리 재작성, 태그 삭제)은 **먼저 묻는다.**
  자동으로 하는 것은 평범한 커밋과 푸시까지다.

---

## 2. 검증

```bash
tools/test.sh        # 전체 테스트 (종료 코드로 판정한다)
tools/package.sh     # 웹스토어용 zip + 사전 점검
```

- **소스를 고쳤으면 `src/shared/defaults.js` 의 `GT_BUILD` 를 올린다.**
  브라우저에서 `:version` 이 그 값을 찍는다 — 확장이 다시 로드됐는지 이걸로 확인한다.
- 브라우저 확인은 `chrome://extensions` 의 ↻ 를 눌러야 반영된다.
  **그 버튼은 내가 누를 수 없다.** 필요하면 사용자에게 부탁한다.

---

## 3. 이 저장소에서 특히 중요한 것

전역 지침 §2("확인하지 않은 것을 단정하지 않는다")가 여기서는 더 엄격하다.
이 확장은 **원본이 문서로 주지 않는 내부 구조**에 기대고 있다.

- 원본의 동작은 **반드시 브라우저에서 실측한다.** 문헌이나 기억으로 쓰지 않는다.
- 실측한 값은 `docs/issue/` 나 `docs/plan/` 에 **관측한 그대로** 남긴다.
- `[확정]` / `[가정]` / `[미정]` 을 구분한다. 미정에는 **어떻게 확인하는지** 를 같이 적는다.
- **없는 것을 있는 척 만들지 않는다.** 확인 못 한 기능은 만들지 말고 미정으로 남긴다.
- 바뀔 수 있는 값(도구 이름, 내부 식별자)을 **판별자로 쓰지 않는다.** 주석에만 남긴다.

## 4. 회귀 테스트

- 고쳤으면 `test/` 에 검사를 남긴다. 이 저장소의 테스트는 대부분 이슈에서 나왔다.
- **새로 쓴 검사는 일부러 깨뜨려 실패하는지 확인한다.** 통과만 보고 넘어가지 않는다 —
  실제로 아무것도 검증하지 못하는 검사를 여러 번 썼다.

## 5. 문구

- 사용자에게 보이는 모든 문구는 **존댓말**이다.
- 문구는 `src/shared/i18n.js` 의 사전에서 꺼낸다. `ko` 와 `en` 을 같이 넣는다.
- **이모지를 쓰지 않는다.**
